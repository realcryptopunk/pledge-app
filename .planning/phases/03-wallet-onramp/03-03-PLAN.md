---
phase: 03-wallet-onramp
plan: 03
type: execute
depends_on: []
files_modified: [Core/Models.swift, Core/AppState.swift, Features/Setup/SetupFlowState.swift, Features/Setup/SetupContainerView.swift, Features/Setup/RiskProfileView.swift, Features/Portfolio/PortfolioView.swift]
---

<objective>
Implement a 3-tier risk profile system that determines how failed-habit stakes are allocated. Users select their risk tolerance during setup, and the portfolio view reflects their chosen allocation strategy.

Purpose: Let users choose their investment risk level — from conservative T-bills to aggressive venture-style tokenized equities.
Output: RiskProfile model, interactive selector UI in setup flow, risk-aware portfolio display.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@Core/Models.swift
@Core/AppState.swift
@Features/Setup/SetupFlowState.swift
@Features/Setup/SetupContainerView.swift
@Features/Portfolio/PortfolioView.swift

**Risk tiers defined by user:**
- Tier 1 (Conservative): 3/6 month US Treasury Bills — lowest risk, guaranteed returns, short maturity
- Tier 2 (Moderate): Pendle Principal Tokens + fixed duration on-chain RWAs — fixed yield, stablecoin-based, medium maturity
- Tier 3 (Aggressive): Private companies tokenized by Robinhood — venture-style risk/reward, equity upside, illiquid

**Current portfolio state:**
- PortfolioView has hardcoded AllocationItem array with 5 Pendle PT tokens
- Static allocations: PT-sUSDai (35%), PT-USDai (25%), PT-thBILL (20%), PT-weETH (12%), PT-gUSDC (8%)
- weightedAPY computed from these allocations

**Current setup flow:**
- SetupStep enum: chooseHabits → configureHabits → setStakes → deposit → permissions → success
- SetupFlowState is @Observable class
- SetupContainerView switches on flowState.currentStep

**Design system:**
- Cards: AccentCard, CleanCard, FlatCard
- Buttons: Primary, Accent, Secondary, Ghost, Pill
- Animations: springBounce, quickSnap, staggerIn
- Typography: pledgeHero, pledgeTitle, pledgeBody, pledgeCaption
- Haptics: PPHaptic.light(), .medium(), .heavy()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RiskProfile model and allocation data structures</name>
  <files>Core/Models.swift, Core/AppState.swift, Features/Setup/SetupFlowState.swift, Features/Setup/SetupContainerView.swift</files>
  <action>
**Models.swift — Add after PhotoVerificationResult:**

1. Create `RiskProfile` enum (String, Codable, CaseIterable):
   - `.conservative` — "Conservative"
   - `.moderate` — "Moderate"
   - `.aggressive` — "Aggressive"

2. Add computed properties on RiskProfile:
   - `title: String` — "Conservative", "Moderate", "Aggressive"
   - `subtitle: String` — "US Treasury Bills", "Fixed Yield DeFi", "Tokenized Ventures"
   - `description: String` — brief explanation of each tier
   - `icon: String` — SF Symbol: "building.columns.fill", "chart.line.uptrend.xyaxis", "flame.fill"
   - `expectedReturn: String` — "4-5% APY", "5-9% APY", "15-50%+ (variable)"
   - `riskLevel: String` — "Very Low", "Low-Medium", "High"
   - `lockPeriod: String` — "3-6 months", "1-12 months", "12-36 months"
   - `color: Color` — .pledgeGreen, .pledgeBlue, .pledgeOrange

3. Add `allocations: [AllocationItem]` computed property on RiskProfile that returns the appropriate allocation mix:
   - Conservative: [("T-Bill 3M", "US Treasury 3-Month", "🏛️", 0.60, 4.2, .pledgeGreen), ("T-Bill 6M", "US Treasury 6-Month", "🏛️", 0.40, 4.8, .pledgeBlue)]
   - Moderate: [("PT-sUSDai", "Pendle sUSDai", "🔒", 0.35, 9.0, .pledgeBlue), ("PT-USDai", "Pendle USDai", "🏦", 0.25, 5.5, .pledgeViolet), ("PT-thBILL", "Pendle T-Bills", "📜", 0.20, 5.7, .pledgeGreen), ("PT-weETH", "Pendle weETH", "⟠", 0.12, 2.7, .pledgeOrange), ("PT-gUSDC", "Pendle gUSDC", "💵", 0.08, 6.6, .cyan)]
   - Aggressive: [("RH-SpaceX", "SpaceX (Robinhood)", "🚀", 0.30, 0, .pledgeOrange), ("RH-Stripe", "Stripe (Robinhood)", "💳", 0.25, 0, .pledgeViolet), ("RH-Databricks", "Databricks (Robinhood)", "📊", 0.20, .pledgeBlue), ("RH-Anthropic", "Anthropic (Robinhood)", "🧠", 0.15, 0, .pledgeGreen), ("PT-thBILL", "Pendle T-Bills (Buffer)", "📜", 0.10, 5.7, .cyan)]
   Note: For aggressive tier, fixedAPY is 0 because returns are variable/equity-based. Show "Variable" instead of a percentage in the UI.

**AppState.swift:**
1. Add `@AppStorage("riskProfile") var riskProfile: RiskProfile = .moderate` (RiskProfile needs RawRepresentable conformance for @AppStorage — it already has String raw value via Codable)

**SetupFlowState.swift:**
1. Add `var selectedRiskProfile: RiskProfile = .moderate` property
2. Add `.riskProfile` case to SetupStep enum — insert it AFTER `.setStakes` and BEFORE `.deposit`:
   - New order: chooseHabits → configureHabits → setStakes → riskProfile → deposit → permissions → success

**SetupContainerView.swift:**
1. Add `case .riskProfile: RiskProfileView(flowState: flowState)` to the switch
2. In `commitSetup()`, add: `appState.riskProfile = flowState.selectedRiskProfile`
  </action>
  <verify>Project builds: xcodebuild -project PledgeApp.xcodeproj -scheme PledgeApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</verify>
  <done>RiskProfile enum exists with 3 tiers, allocation data, and display properties. SetupStep includes riskProfile step. AppState persists risk profile.</done>
</task>

<task type="auto">
  <name>Task 2: Build RiskProfileView and update PortfolioView</name>
  <files>Features/Setup/RiskProfileView.swift, Features/Portfolio/PortfolioView.swift</files>
  <action>
**RiskProfileView.swift (NEW FILE):**

Create an interactive 3-tier risk profile selector for the setup flow.

Layout:
- Top bar with back chevron (flowState.goBack())
- Title: "Choose your strategy" (pledgeTitle) with subtitle "Where should failed stakes be invested?" (pledgeBody, secondary)
- 3 vertically stacked cards, one per RiskProfile case
- Each card shows:
  - Left: icon (SF Symbol) in a themed circle
  - Center: title + subtitle + expected return + lock period
  - Right: radio indicator (filled circle if selected)
  - Selected card: accent border with theme gradient, slightly elevated shadow
  - Unselected: subtle border, flat
- Use CleanCard or custom card styling matching app aesthetic
- Bottom: "Continue" primary button (flowState.goForward()), disabled until a profile is selected (default .moderate so always enabled)
- Tapping a card: PPHaptic.medium(), animate selection with .quickSnap

Style details:
- Cards should feel like option tiles — clean, spaced, with clear visual hierarchy
- Selected state: gradient border (theme.buttonTop → theme.buttonBottom), inner glow
- Each tier's icon circle uses the tier's color property
- Show risk level badge on each card (e.g., "Very Low" in green pill for conservative)
- staggerIn(index:) for entry animation on each card

Bind selection to `flowState.selectedRiskProfile`.

**PortfolioView.swift updates:**

1. Replace the static `allocations` array with a computed property that reads from AppState:
   ```swift
   private var allocations: [AllocationItem] {
       appState.riskProfile.allocations
   }
   ```
2. Remove the `static let allocations` constant
3. Update `weightedAPY` to handle aggressive tier where fixedAPY is 0:
   - If riskProfile is .aggressive, show "Variable" or "~25% target" instead of computed APY
   - For conservative and moderate, keep the weighted average calculation
4. Add a small label somewhere in the portfolio header showing the current risk tier (e.g., "Moderate Risk" pill badge)
  </action>
  <verify>Project builds without errors. RiskProfileView.swift exists.</verify>
  <done>RiskProfileView shows 3 interactive tier cards in setup flow. PortfolioView displays risk-tier-appropriate allocations. Both build without errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] RiskProfile enum with 3 tiers in Models.swift
- [ ] Each tier has allocations, display properties, colors
- [ ] SetupStep includes .riskProfile between setStakes and deposit
- [ ] RiskProfileView renders 3 selectable cards
- [ ] PortfolioView shows allocations from user's selected risk profile
- [ ] AppState persists risk profile via @AppStorage
- [ ] Project builds without errors
</verification>

<success_criteria>

- All 3 risk tiers are fully defined with allocation data
- Setup flow includes risk profile selection step
- Portfolio view dynamically reflects chosen risk tier
- UI follows design system (cards, animations, typography, haptics)
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-wallet-onramp/03-03-SUMMARY.md`
</output>
