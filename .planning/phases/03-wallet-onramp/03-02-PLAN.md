---
phase: 03-wallet-onramp
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [PledgeApp.swift, Core/AppState.swift, Features/Auth/SignInWithAppleView.swift]
---

<objective>
Migrate the auth flow from Supabase Apple Sign-In to Privy phone OTP, and create an embedded Ethereum wallet on first login. The auth view becomes a phone number + OTP verification flow.

Purpose: Connect users to their embedded wallets via Privy's phone auth, replacing the current placeholder Supabase auth.
Output: Working auth flow with phone OTP → wallet creation. Wallet address persisted in AppState.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-wallet-onramp/03-01-SUMMARY.md
@PledgeApp.swift
@Core/AppState.swift
@Features/Auth/SignInWithAppleView.swift
@Core/Services/PrivyManager.swift
@Core/Services/AuthService.swift

**Current auth flow:**
- PledgeApp.swift routes: Splash → Onboarding → SignInWithAppleView → Setup → Main
- AppState.isAuthenticated is driven by authService.isAuthenticated (Supabase)
- SignInWithAppleView uses ASAuthorizationAppleIDRequest + Supabase signInWithIdToken
- AuthService uses Supabase client for session management
- Guest mode exists (signInAsGuest) — keep this for dev

**Target auth flow:**
- PledgeApp.swift routes: Splash → Onboarding → PrivyAuthView (phone OTP) → Setup → Main
- AppState.isAuthenticated driven by PrivyManager.isAuthenticated
- PrivyAuthView: enter phone → receive OTP → verify → wallet created → proceed
- Keep guest mode for dev (PrivyManager should support it)

**Design patterns to follow:**
- Keep FluidPurpleGlow animation from current SignInWithAppleView (it's beautiful)
- Use existing design system: pledgeHero, pledgeBody, pledgeCaption, embossed, staggerIn
- Theme-aware via @Environment(\.themeColors)
- WaterBackgroundView as base
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PrivyManager into AppState and PledgeApp lifecycle</name>
  <files>Core/AppState.swift, PledgeApp.swift</files>
  <action>
**AppState changes:**
1. Add `let privyManager = PrivyManager.shared` property (alongside existing authService)
2. Add `@AppStorage("walletAddress") var walletAddress: String = ""` for persistence
3. Add `@Published var userPhone: String = ""`
4. In init(), call `Task { await privyManager.initialize() }` to init Privy SDK
5. Modify the `isAuthenticated` published property to be driven by privyManager instead of authService:
   - Add a Combine sink on privyManager.$isAuthenticated that updates self.isAuthenticated
   - Similarly sink privyManager.$walletAddress to update self.walletAddress
6. Keep authService property for now (don't delete — gradual migration), but it won't drive routing anymore

**PledgeApp changes:**
1. Replace `appState.authService.isLoading` with `appState.privyManager.isLoading` in the splash condition
2. Replace `SignInWithAppleView()` with `PrivyAuthView()` in the routing switch
3. Keep all other routing logic identical (onboarding, setup, main)
4. Keep the .task and .onReceive modifiers as-is

Do NOT remove AuthService.swift or the Supabase dependency yet — keep for potential future data use.
  </action>
  <verify>Project builds: xcodebuild -project PledgeApp.xcodeproj -scheme PledgeApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</verify>
  <done>AppState uses PrivyManager for auth state. PledgeApp routes to PrivyAuthView. Builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create PrivyAuthView with phone OTP flow</name>
  <files>Features/Auth/PrivyAuthView.swift</files>
  <action>
Create a new PrivyAuthView.swift that replaces SignInWithAppleView. Two-step flow: phone entry → OTP verification.

**Structure:**
- @EnvironmentObject var appState: AppState
- @Environment(\.themeColors) var theme
- @State var step: AuthStep (.phone, .otp)
- @State var phoneNumber: String
- @State var otpCode: String
- @State var isLoading: Bool
- @State var errorMessage: String?

**Phone step UI:**
- Keep FluidPurpleGlow and WaterBackgroundView from SignInWithAppleView (copy the animation)
- Logo: checkmark.shield.fill with gradient (same as current)
- "Welcome to" + "Pledge" hero text with embossed effect
- Subtitle: "Stake your habits.\nBuild your future."
- Phone number text field: custom styled with country code (+1 US), large monospaced font
- "Continue" primary button — calls `appState.privyManager.sendOTP(to: formattedPhone)`
- "Skip for now →" ghost button — calls `appState.privyManager.signInAsGuest()` (add this method to PrivyManager if not present — simply sets isAuthenticated=true, walletAddress="" for dev)
- Terms text at bottom

**OTP step UI:**
- Same background (WaterBackgroundView + FluidPurpleGlow)
- Back chevron to return to phone step
- "Enter verification code" header
- "Sent to {phoneNumber}" subtitle
- 6-digit OTP field (individual boxes, auto-advance, monospaced)
- Auto-submit when 6 digits entered OR "Verify" button
- Calls `appState.privyManager.verifyOTP(code:phoneNumber:)` — this also creates wallet
- Loading state with spinner
- Error display
- "Resend code" link (calls sendOTP again)

**Transitions:** Use .springBounce animation between steps. staggerIn(index:) for entry animations.

**Phone formatting:** Accept digits only, format display as (XXX) XXX-XXXX, send as +1XXXXXXXXXX (E.164). Only support US numbers for now.

Do NOT delete SignInWithAppleView.swift — keep as reference. The routing in PledgeApp.swift already points to PrivyAuthView from Task 1.
  </action>
  <verify>Project builds without errors. PrivyAuthView.swift exists with both phone and OTP steps.</verify>
  <done>PrivyAuthView renders phone entry and OTP verification. Both steps have proper UI with design system. Guest mode works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Privy phone auth flow replacing Apple Sign-In, with embedded wallet creation on successful OTP verification</what-built>
  <how-to-verify>
    1. Run the app in Simulator (or Xcode preview)
    2. Complete onboarding (or skip)
    3. Verify: PrivyAuthView shows with phone number input (not Apple Sign-In)
    4. Verify: Design matches existing aesthetic (fluid purple glow, water background, embossed text)
    5. Verify: "Skip for now" button works (guest mode)
    6. Note: Real phone auth won't work until Privy Dashboard credentials are configured — visual verification only for now
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AppState.isAuthenticated driven by PrivyManager
- [ ] PledgeApp routes to PrivyAuthView (not SignInWithAppleView)
- [ ] PrivyAuthView has phone entry + OTP verification steps
- [ ] Guest mode still works
- [ ] Project builds without errors
- [ ] UI follows design system (themes, animations, typography)
</verification>

<success_criteria>

- Auth flow uses Privy phone OTP instead of Apple Sign-In
- Wallet address stored in AppState on successful auth
- Guest mode preserved for development
- Design quality matches existing app aesthetic
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-wallet-onramp/03-02-SUMMARY.md`
</output>
